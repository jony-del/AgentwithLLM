"""Cross-platform installer support for Polaris.

This package is intentionally stdlib-only: the shell entrypoints use a uv-managed
Python to run it before the project itself has been installed.
"""

